# Scripts

This folder contains seed-data tools, developer utilities, bulk test-data generators, and local replay or voice-test scripts.

Current script:

- `run_voice_test.py` generates a first local clinic-agent audio sample using OpenAI text-to-speech.
